{
  "name": "@imbrace/cli",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "imbrace": "./bin/run.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/commands",
    "imbrace": "bun run bin/run.js",
    "postpack": "rm -f oclif.manifest.json",
    "prepack": "oclif manifest"
  },
  "dependencies": {
    "@imbrace/sdk": "1.0.2",
    "@inquirer/prompts": "^7.10.1",
    "@oclif/core": "^4.2.0",
    "@oclif/plugin-autocomplete": "^3.2.0",
    "@oclif/plugin-help": "^6.2.0",
    "conf": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "oclif": "^4.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "oclif": {
    "bin": "imbrace",
    "dirname": "imbrace",
    "commands": "./dist/commands",
    "topicSeparator": " ",
    "plugins": [
      "@oclif/plugin-help",
      "@oclif/plugin-autocomplete"
    ],
    "topics": {
      "data-board": {
        "description": "Manage data boards — run `imbrace data-board --help` to see all commands"
      },
      "ai-agent": {
        "description": "Manage AI agents — run `imbrace ai-agent --help` to see all commands"
      },
      "workflow": {
        "description": "Manage workflows (Activepieces) — run `imbrace workflow --help` to see all commands"
      },
      "workflow:piece": {
        "description": "Discover available integrations (pieces) — run `imbrace workflow piece --help`"
      },
      "workflow:node": {
        "description": "Manage workflow nodes (trigger + actions) — run `imbrace workflow node --help`"
      },
      "workflow:conn": {
        "description": "Manage connections (credentials for external services) — run `imbrace workflow conn --help`"
      },
      "workflow:folder": {
        "description": "Organize workflows into folders — run `imbrace workflow folder --help`"
      },
      "workflow:mcp": {
        "description": "Manage MCP (Model Context Protocol) servers — run `imbrace workflow mcp --help`"
      }
    }
  },
  "files": [
    "/bin",
    "/dist"
  ]
}
